# Replace the URL below with your actual Cloudflare backend tunnel URL
# after running: cloudflared tunnel --url http://localhost:5001
VITE_API_URL=https://YOUR-BACKEND-TUNNEL-URL.trycloudflare.com/api
